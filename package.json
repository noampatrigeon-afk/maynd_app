{
  "name": "maynd-app",
  "version": "0.7.0",
  "description": "MAYND — application de developpement personnel et de performance mentale. Prototype autonome en HTML/CSS/JavaScript, sans framework.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node watch.mjs",
    "test": "node build.mjs && node tests/run.mjs",
    "start": "node build.mjs && node serve.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "engines": { "node": ">=18" }
}
